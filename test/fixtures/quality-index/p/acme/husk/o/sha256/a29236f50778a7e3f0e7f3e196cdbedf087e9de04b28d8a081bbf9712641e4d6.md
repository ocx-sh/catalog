# husk

**husk** is retained for provenance only. A packaging defect in every published
build caused the extracted binary to miss its runtime shared libraries on
musl-based systems, so all versions were withdrawn.

Do not install husk. There is no drop-in successor; re-evaluate whether the
capability is still needed before adopting an alternative.
