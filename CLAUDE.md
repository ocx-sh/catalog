# CLAUDE.md

<!--
  Do not add project context here. This file exists only as Claude Code's
  import hook; every other agent reads AGENTS.md directly. Always edit
  AGENTS.md instead — content added here is invisible to Codex CLI and
  every other AGENTS.md-aware tool, and a second copy would drift.
-->

Project context lives in [`AGENTS.md`](./AGENTS.md) — one file, every
agent. **Edit that file, never this one**; this is only the Claude Code
import hook.

@AGENTS.md
