{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "packageRules": [
    {
      "description": "SHA-pin every GitHub Actions reference — matches this repo's own workflow-security invariant.",
      "matchManagers": ["github-actions"],
      "pinDigests": true
    },
    {
      "description": "Group routine npm minor/patch bumps into one PR. vitepress/vue are excluded here and handled by the rule below.",
      "matchManagers": ["npm"],
      "matchUpdateTypes": ["minor", "patch"],
      "excludePackageNames": ["vitepress", "vue"],
      "groupName": "npm minor/patch"
    },
    {
      "description": "vitepress ships on an alpha channel (2.0.0-alpha.*) and vue is pinned in step with it — deliberately NOT matched above, so each gets its own ungrouped PR for manual review before merge.",
      "matchManagers": ["npm"],
      "matchPackageNames": ["vitepress", "vue"]
    }
  ],
  "lockFileMaintenance": {
    "enabled": true,
    "extends": ["schedule:weekly"]
  }
}
